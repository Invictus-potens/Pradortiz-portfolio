services:
  app:
    build:
      context: .
    container_name: pradortiz-portfolio
    ports:
      - "48096:48096"
    restart: unless-stopped
    networks:
      - tool_net

networks:
  tool_net:
    external: true